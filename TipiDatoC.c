#include <studio.h>

int main(){
    int numero = 1 ; // int = numero intero senza parte decimale a 32bit
    long Grande_numero = 1000000000000; // long = numero intero senza parte decimale a 64bit
    char lettera = 'a' ; // char = singolo carattere (a,b,c)
    char stringa[] = "Ciao"; // Stringa = array di char 
    float numero_decimale = 1.00; // float = numero con parte decimale
    double numero_decimale2 = 2.44; // double = numero con parte decimale di due posizioni dopo la virgola
    int array[5] = {1,2,3,4,5}; // array = può contenere più dati dello stesso tipo dichiarazione [tipo] nome_array [[numero di posizioni]] = [{dati}], le posizioni partono da 0 a n  
}
    